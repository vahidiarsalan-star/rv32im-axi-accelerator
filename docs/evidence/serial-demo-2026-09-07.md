# Serial demo evidence — 2026-09-07

Generated by `python sim/run_tests.py --waves`. These are selected console
transcripts from the **RTL simulation**, with extra console blank lines and
simulator bookkeeping omitted. Serial bytes are sampled on `uart_tx_pin`;
this is not a physical-board terminal capture.

Source identity and tool versions: [verification snapshot](verification-2026-09-07.json).
The full logs are regenerated under `build/verification/`.

## Monitor acceptance and rejection

Source log: `uart_monitor.log`. The test tries G without an image, an out-of-range
load, a bad checksum, G after that rejected load, and finally a valid six-word
program. It also checks the six uploaded RAM words.

```text
RV32I UART monitor
L addr words sum + 8-hex words
D addr words | G addr | H
App: 00000800..00000DFF
> ERR NO IMAGE
> ERR RANGE
> ERR SUM 00000013
> ERR NO IMAGE
> OK 00000006 WORDS
> GO 00000800
!
*** UART MONITOR PASS *** (negative commands + serial TX + RAM)
```

## Freshly compiled arithmetic application

Source log: `uart_hello_app.log`. `hello.c` uses volatile operands so the compiler
cannot fold the initial arithmetic results into string constants.

```text
> OK 0000009A WORDS
> GO 00000800
RV32I C arithmetic demo
37 + 12 = 49
37 - 12 = 25
tick
*** UART HELLO APP PASS ***
```

## Freshly compiled echo application

Source log: `uart_echo_app.log`. After application handoff, the driver sends
`Echo OK!` plus CR/LF and checks those bytes on the outgoing serial pin.

```text
> OK 00000052 WORDS
> GO 00000800
UART RX echo app. Type text:
Echo OK!
*** UART ECHO APP PASS ***
```

Word counts and binary sizes depend on the compiler. Functional text expectations
are checked by the testbench; these excerpts are supporting documentation.
